# Ensure consistent line endings for all text files
* text=auto

# Specific configuration for JSON files
*.json text eol=lf

# Specific configuration for shell scripts
*.sh text eol=lf

# Specific configuration for Windows batch files
*.bat text eol=crlf